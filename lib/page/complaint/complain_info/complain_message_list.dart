import 'package:flutter/material.dart';
import 'package:get/get.dart';

class ComplainMessageListWidget extends StatelessWidget {
  const ComplainMessageListWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView(

      children: [
        Text("hello dev"),
        Text("hello dev"),
        Text("hello dev"),
        Text("hello dev"),
      ],
    );
  }
}
